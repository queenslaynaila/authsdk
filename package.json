{
  "name": "betkumi-afiliates-auth-sdk",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "esbuild auth.ts --bundle --minify --format=iife --platform=browser --global-name=Auth --outfile=dist/auth.min.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.10.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
